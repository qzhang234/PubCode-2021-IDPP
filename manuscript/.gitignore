# build/ is derived: everything in it is regenerated by `make papers`.
# The finished PDFs are kept in git so collaborators can read them
# without a TeX installation; the LaTeX aux files are not.
build/*
!build/*.pdf
